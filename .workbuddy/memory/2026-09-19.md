# 2026-09-19 工作日志

## 中节能低碳园区微电网数字化平台 - 部署到 GitHub Pages
- 仓库：`chengfei6840/cecep-microgrid-platform`（public）
- 外网地址：`https://chengfei6840.github.io/cecep-microgrid-platform/`
- 部署方式：GitHub Actions 自动构建（push main 触发）；vite `base` 由 `VITE_BASE=/cecep-microgrid-platform/` 注入；SPA fallback 用 `cp dist/index.html dist/404.html` + `touch dist/.nojekyll`。
- 踩坑记录：`actions/configure-pages@v5` 的 `enablement:true` 在 GITHUB_TOKEN 下**首次启用 Pages** 报 `Resource not accessible by integration`（GITHUB_TOKEN 只能部署已存在 site，不能创建 site）。改用 `gh api repos/<owner>/<repo>/pages -X POST -f build_type=workflow` 手动启用一次，并在 workflow 中删除该步骤，后续 deploy-pages@v4 正常工作。
- 验证：home / JS / CSS / 404.html 均 200；深链接 `/dashboard` 经 404.html fallback 正常渲染（含 `id="root"`）。
- 注意：原型为纯前端 mock（src 无 fetch/外部 API 调用），Gemini/AI 仅为 UI 演示开关，静态部署后全部页面可用，无需 API key。
